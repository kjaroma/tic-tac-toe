version: "3.8"
services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres
    restart: always
    ports:
      - '5432:5432'
    networks:
      - app
    env_file:
      - ./postgres.env

networks:
  app:
    name: app
    driver: bridge
